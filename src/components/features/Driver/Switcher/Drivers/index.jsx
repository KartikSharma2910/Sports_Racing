import { Box } from "@mui/material";
import { Card } from "components/common";

const Drivers = () => {
  return (
    <Box>
      <Card as="DriverCard" />
    </Box>
  );
};

export default Drivers;
