const styles = {
  wrapper: {
    display: "flex",
    flexDirection: "column",
    gap: "12px",
  },

  labeler: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
  },

  label: {
    textTransform: "uppercase",
  },
};

export default styles;
